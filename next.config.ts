import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "encrypted-tbn0.gstatic.com", "images.unsplash.com", "www.rciaquatech.com"],
  },
};

export default nextConfig;
